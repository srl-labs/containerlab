# yaml-language-server: $schema=../../schemas/clab.schema.json
name: link-vars-ip-invalid-v6-addr
topology:
  nodes:
    n1:
      kind: linux
      image: alpine
    n2:
      kind: linux
      image: alpine
  links:
    - endpoints: [n1:eth1, n2:eth1]
      vars:
        ipv6: [n1:INVALID]

