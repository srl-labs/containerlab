name: clab
# node name will be clab-srl
prefix: __lab-name

topology:
  nodes:
    srl:
      kind: nokia_srlinux
      image: ghcr.io/nokia/srlinux:${SRL_VERSION:=latest}
      type: ${SRL_TYPE:=ixrd3l}
